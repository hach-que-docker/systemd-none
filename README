To run this image:

```
docker run 
  -d \
  -P \
  <image>
```

```
-d = detach from tty
-P = map exposed ports over NAT
<image> = the name of the image
/init = start simple init
```

You can use this image to run application if you don't want to use cgroups or privileged mode.
